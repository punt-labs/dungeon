---
description: "Shorthand for /dungeon — play a text adventure game"
---

This is a shorthand alias. Run the dungeon skill now with these arguments:

**$ARGUMENTS**

Use the Skill tool to invoke the `dungeon` skill with the arguments above. Pass `$ARGUMENTS` as the `args` parameter.
